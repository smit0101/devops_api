# Database Configuration
MYSQL_ROOT_PASSWORD=secret_root_password
MYSQL_DATABASE=devopsdb
MYSQL_USER=devops_user
MYSQL_PASSWORD=secret_user_password

# Spring Boot DataSource Configuration
# NOTE: Ensure these match the MYSQL_ values above
SPRING_DATASOURCE_USERNAME=devops_user
SPRING_DATASOURCE_PASSWORD=secret_user_password

# Optional: GitHub Token (Can also be set via UI Settings)
# GITHUB_TOKEN=
